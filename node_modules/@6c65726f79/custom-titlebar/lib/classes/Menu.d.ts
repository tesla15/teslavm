import MenuItem from './MenuItem';
export default class Menu {
    menuItems: Array<MenuItem>;
    element: HTMLDivElement;
    subMenu: Menu | null;
    isSubMenu: boolean;
    activeMenu: number;
    constructor(items: Array<any>, submenu?: boolean);
    isSubMenuOpened(): boolean;
    openSubMenu(submenu: Record<string, any>, index: number): void;
    closeSubMenu(main?: boolean): void;
    uncheckRadioGroup(itemIndex: number): void;
    private getItemGroup;
    private getGroupItems;
}
